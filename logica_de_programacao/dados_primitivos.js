const name = "jose"; //string
const number = 10; // number -> parseInt 
const numberfloat = 10.5 // number -> parseFloat
let nomeTwo; //undefined -> não aponta pra local nenhum da memoria
let nomeTree = null; // nulo -> não aponta pra local nenhum da memória('utilizavel') 
const bool = false; // valor boolean -> true || false
const lista = []; // array -> estrutura de dados